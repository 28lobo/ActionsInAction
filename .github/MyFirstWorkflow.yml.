name: My First Workflow
on:
  push:
    branches:
      -main
  workflow_dispatch
jobs:
  MyFirstJob:
    runs-on: ubuntu-latest
    steps:
      - name: My first step
        run: echo "Hello World"
